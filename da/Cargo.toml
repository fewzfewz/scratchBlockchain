[package]
name = "da"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"
sha2 = "0.10"
# Note: For production, would use rust-kzg or c-kzg bindings
# For this MVP, we'll implement a simplified version
